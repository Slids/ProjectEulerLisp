(defvar  first 1)
(defvar second 2)
(defvar sum 2)
(loop
 (setq total (+ first second))
 (if (> total 55)
     (return 0))
 (if (= (mod total 2) 0)
     (setq sum (+ total sum)))
 (setq temp first)
 (setq first second)
 (setq second (+ temp first))
)
(write sum)
